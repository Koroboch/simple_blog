<div class="container">
  <br>
  <div class="row">
    <div class="col col-md-2"></div>
    <div class="col col-md-7">
      <%= turbo_frame_tag @article do %>
      <article class="card">
        <div class="card-header bg-light"><h5><b><%= @article.title%></b></h5></div>
        <div class="card-body">
          <section class="card-text">
            <p><%=@article.text%></p>
            <p6><%="Published #{@article.username}, in #{@article.created_at.strftime("%Y-%m-%d %H:%M")}"%></p6>
          </section>
          <%if user_signed_in?%>
            <%if @article.username == current_user.username%>
              <div class="btn btn-warning btn-sm">
                <%= link_to 'Edit', edit_article_path(@article), :class => "btn", :style => "--bs-btn-padding-y: .1rem; --bs-btn-padding-x: .15rem; --bs-btn-font-size: .65rem;"%>
              </div>
              <div class="btn btn-danger btn-sm">
                <%= button_to 'Delete', (@article), method: :delete, form: { data: { 'turbo-confirm': 'Вы уверены?' } }, :class => "btn", :style => "--bs-btn-padding-y: .1rem; --bs-btn-padding-x: .15rem; --bs-btn-font-size: .65rem;"%>
              </div>
            <% end %>
          <% end %>
          </div>
        </article>
      <% end %>
      <article class="card mt-1">
        <%= render 'comment' %>
        <div class="card-body">
          <div class="card-text">
            <% if user_signed_in?%>
              <%= render 'shared/add_comment' %>
            <%else%>
              <p5>Need sign in. <%= link_to 'Sign in', new_user_session_path%></p5>
            <%end%>
          </div>
        </div>
      </article>
      <br>
    </div>
    <div class="col col-md-4"></div>
  </div>
</div>